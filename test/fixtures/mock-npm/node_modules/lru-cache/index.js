module.exports = function(cfg) {
  // To get full coverage in test of fetch-package-metadata, must make use
  // of the 'length' property of the config object:
  if (cfg && cfg.length) cfg.length({ _contentLength: 42 })
}

