module.exports = function(opts, callback) {
  // Return the same generic entry list every time
  const p = Promise.resolve([
    'LICENSE',
    'index.js',
    'package.json',
    'README.md'
  ])
  return callback ? p.then(list => callback(null, list)) : p
}
