let testConfig

const getWorkspaceNodes = (tree, workspaces, log) => {
  const wsMap = tree.workspaces
  if (!wsMap) {
    return []
  }
  return testConfig
}

module.exports = getWorkspaceNodes
module.exports.setTestConfig = (data) => testConfig = data