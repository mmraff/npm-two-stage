module.exports = function(tgtPath, cb) {
  /*
     The actual module builds an object (tree), the important part of which is
	 the children property. The caller (addBundled() in fetch-package-metadata)
	 sets the _bundled property of its pkg object with that.
	 This module sends the tree (or nothing) through the callback.
   */
  cb()
}

